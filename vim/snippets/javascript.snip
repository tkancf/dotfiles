snippet     forof
abbr        forof
options     head
  for (var ${1:value} of ${2:object}) {
    ${0:TARGET}
  }

snippet     for1
abbr        for normal
options     head
  for (var ${1:i} = ${2:start}; $1 < ${3:end}; $1++) {
    ${0:TARGET}
  }
